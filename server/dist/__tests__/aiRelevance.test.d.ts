/**
 * AI 相关性分析准确度评估测试
 *
 * 这套测试用于验证 AI Prompt 对关键词相关性的判断质量。
 * 每个测试用例模拟真实场景：给定关键词 + 内容 → 验证 AI 判断结果是否合理。
 *
 * 运行方式：
 *   npx vitest run src/__tests__/aiRelevance.test.ts
 *
 * 注意：需要配置 OPENROUTER_API_KEY 环境变量才能调用真实 AI。
 * 如果未配置，测试会跳过（不会失败）。
 */
export {};
//# sourceMappingURL=aiRelevance.test.d.ts.map